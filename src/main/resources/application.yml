server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Salbi1/config-repo.git
    eureka:
      client:
        service-url:
          defaultZone: http://localhost:8761/eureka/
          register-with-eureka: false
          fetch-registry: false